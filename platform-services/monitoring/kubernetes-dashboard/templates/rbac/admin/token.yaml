apiVersion: v1
kind: Secret
metadata:
  name: {{ template "kubernetes-dashboard.fullname" . }}-admin-token
  annotations:
    kubernetes.io/service-account.name: "{{ template "kubernetes-dashboard.fullname" . }}-admin"
type: kubernetes.io/service-account-token

